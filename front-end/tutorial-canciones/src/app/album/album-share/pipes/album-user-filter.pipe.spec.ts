import { AlbumUserFilterPipe } from './album-user-filter.pipe';

describe('AlbumUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
