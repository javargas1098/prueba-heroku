import { CancionUserFilterPipe } from './cancion-user-filter.pipe';

describe('CancionUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CancionUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
